[project]
name = "prompt-engineering-demo-server"
version = "1.0.0"
description = "FastAPI backend for the AI Prompt Engineering Demo"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "openai>=1.30",
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "PyYAML>=6.0",
]

[tool.uv]
package = false